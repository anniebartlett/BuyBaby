<%= simple_form_for [ @user, @review ] do |f| %>
  <%= f.hidden_field :product, :value => "#{@product.id}" %>
  <%= f.input :comment, as: :text, placeholder: "Review seller" %>
  <%= f.input :rating, collection: [ 1, 2, 3, 4, 5] %>
  <%= f.submit "Submit", class: "btn btn-blue mb-3"  %>
<% end %>

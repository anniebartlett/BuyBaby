<<<<<<< HEAD
<div class="container">
  <div class="row mt-3 mb-3">
    <div class="col-md-6">
      <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">          <% @baby.photos.each_with_index do |photo, index| %>
            <div class="carousel-item <%= 'active' if index == 0 %>">
              <%= cl_image_tag photo.key, height: 600, width: 600, crop: :fill, gravity: :center %>
            </div>
          <% end %>
          </div>
        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>
    <div class="col-md-6">
      <br>
      <h1> <%= @product.name.capitalize %></h1>
      <p><strong>Category: </strong><%=@product.category.capitalize %></p>
      <p><strong>Description: </strong><%=@product.description.capitalize %></p>
      <p><strong>Price: </strong>£<%= @product.price %></p>
      <p><strong>Location: </strong>£<%= @product.location.capitalize %></p>
      <div class="links list-inline">
        <p class="list-inline-item"><%= link_to products_path do %>


 <p><%= link_to 'Buy Item', product_buy_path(@product), class: "btn btn-primary" %></p>
 <h3>Seller Reviews:</h3>h3>
 <ul class="list-group m-3">
 </ul>

<div class="cards d-flex flex-wrap justify-content-around mt-5">
      <div class="card mb-5 text-center" style="width: 18rem;">
          <div class="card-body">
            <h3 class="card-title"><%= @product.name.capitalize %></h3>
            <% @product.photos.each do |photo| %>
            <%= cl_image_tag photo.key, alt: "product", height: 300, width: 200, crop: :fill %>
            <% end %>
            <h5 class="card-title"><%= @product.description %></h5>
            <h5 class="card-title"><%= @product.location %></h5>

           <%= link_to "Back to all products", products_path %>
        </div>
      </div>
  </div>


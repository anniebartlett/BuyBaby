<div class="container">
  <div class="py-5 text-left">
    <h2>Checkout </h2>
  </div>
  <div class="row">
    <div class="col-md-4 order-md-2 mb-4">
      <h4 class="d-flex justify-content-between align-items-center mb-3">
        <span class="text-muted">Your Basket</span>
        <span class="badge badge-secondary badge-pill"><%= current_user.orders.find_by(state:"pending").product_orders.count %></span>
      </h4>
      <% @order.product_orders.each do |item| %>
      <ul class="list-group mb-3">
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <% item.product.photos.each_with_index do |photo, index| %>
            <%= cl_image_tag photo.key, height: 100, width: 100, crop: :fill, gravity: :center %>
          <% end %>
          <div class="pl-1">
            <h6 class="my-0"><%= item.product.name %></h6>
            <small class="text-muted"><%= item.product.description %></small>
          </div>
          <span class="text-muted">£<%= item.product.price_cents %></span>
        </li>
        <% end %>
        <li class="list-group-item d-flex justify-content-between">
          <span>Total (GBP)</span>
          <strong><h6>£<%= @order.total_price %></h6></strong>
        </li>
      </ul>
    </div>
    <div class="col-md-8 order-md-1">
      <h4 class="mb-3">Billing address</h4>
      <form class="needs-validation" novalidate="">
        <div class="row">
          <div class="col-md-6 mb-3">
            <label for="firstName">First name</label>
            <input type="text" class="form-control" id="firstName" placeholder="" value="" required="">
            <div class="invalid-feedback">
              Valid first name is required.
            </div>
          </div>
          <div class="col-md-6 mb-3">
            <label for="lastName">Last name</label>
            <input type="text" class="form-control" id="lastName" placeholder="" value="" required="">
            <div class="invalid-feedback">
              Valid last name is required.
            </div>
          </div>
        </div>
        <div class="mb-3">
          <label for="address">Address</label>
          <input type="text" class="form-control" id="address" placeholder="" required="">
          <div class="invalid-feedback">
            Please enter your shipping address.
          </div>
        </div>
        <div class="row">
          <div class="col-md-5 mb-3">
            <label for="country">Country</label>
            <select class="custom-select d-block w-100" id="country" required="">
              <option value="">Choose...</option>
              <option>United Kingdom</option>
            </select>
            <div class="invalid-feedback">
              Please select a valid country.
            </div>
          </div>
          <div class="col-md-4 mb-3">
            <label for="city">City</label>
            <select class="custom-select d-block w-100" id="city" required="">
              <option value="">Choose...</option>
              <option>London</option>
            </select>
            <div class="invalid-feedback">
              Please provide a valid city.
            </div>
          </div>
          <div class="col-md-3 mb-3">
            <label for="postcode">Postcode</label>
            <input type="text" class="form-control" id="postcode" placeholder="" required="">
            <div class="invalid-feedback">
              Postcode code required.
            </div>
          </div>
        </div>
        <div class="custom-control custom-checkbox">
          <input type="checkbox" class="custom-control-input" id="same-address">
          <label class="custom-control-label" for="same-address">Shipping address is the same as my billing address</label>
        </div>
        <hr class="mb-4">
        </form>
        <h6 style="text-align: center;color:#237480;">⭐️ YOU HAVE ITEM(S) FOR SWAP IN YOUR BASKET ⭐️</h6>
        <div class="custom-control custom-checkbox">
          <input type="checkbox" class="custom-control-input" id="swap-options" checked>
          <label style="color:#237480;" class="custom-control-label" for="swap-options">Item(s) Swapped for: TOYS</label>
        </div>
        <div class="custom-control custom-checkbox">
          <input type="checkbox" class="custom-control-input" id="swap" checked>
          <label style="color:#237480;" class="custom-control-label" for="swap">Swap accepted by: <%= @order.products.first.user.nickname %></label>
        </div>
        </form>
        <hr class="mb-4">
        <%= simple_form_for(@order, url: order_path(@order)) do |f| %>
        <div class="form-group">
          <%= f.input :delivery_option, as: :radio_buttons, checked: 'Collect from Seller',
          collection: ['Collect from Seller', 'Standard Free Delivery (3-5 days)', 'Collect from preferred location'] %>
        </div>
        <div class="form-group">
          <%= f.input :payment_option, as: :radio_buttons, checked: 'Cash (if collect from seller)',
          collection: ['Debit card', 'Cash (if collect from seller)', 'Paypal'] %>
        </div>
        <div class="form-group">
          <%= f.input :state, as: :hidden, input_html: { value: "completed" } %>
        </div>
        <hr class="mb-4">
        <div class="row">
          <div class="col-md-12 mb-6 text-left">

            <h5 style="text-align: center;color:#237480;">Payment by Cash</h5>
            <h5 style="text-align: center;color:#237480;">CONTACT SELLER FIRST: <a href="https://wa.me/447709793973?text=Please%20contact%20me%20about%20this%20product%20listing" target="blank"><i class="fab fa-whatsapp"></i></a></h5>
          </div>
        </div>
        <div class="row">
         <div class="col-md-12 mb-6">
          <hr class="mb-4">
           <%= f.button :submit, 'CONFIRM ORDER', class: 'btn btn-block btn-primary mb-5 text-white' %>
        <% end %>
        </div>
      </div>
    </div>
</div>
